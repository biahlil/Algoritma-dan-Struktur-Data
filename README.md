# Algoritma-dan-Struktur-Data

My Practice for DSA and Making It's Visualization on TUI


### How To Clone This Repo

git clone --recurse-submodules `<repository-url>`

eed up the download by fetching them in parallel by adding `--jobs`e.g., `git clone --recurse-submodules --jobs 4 <repository-url>`
